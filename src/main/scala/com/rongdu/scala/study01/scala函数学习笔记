scala 函数/方法的定义

//有参数 有返回的方法
def 方法名(参数值 : 参数类型): 返回值类型 = {
    //方法体
    //方法体内部 最后一行是返回值,return 关键字不是必须的(可用可不用).
}

//有参数 无返回值
def 方法名(参数值:参数类型): Unit={
}

//无参数方法, 调用的时候  可以直接使用 方法名调用  不需要加()
def 方法名(): 返回值类型={
}

//无参数 无返回值方法
def 方法名(): Unit={
}

//默认参数  可以为方法的参数指定默认值.


//循环表达式
    to
    Range
    until
